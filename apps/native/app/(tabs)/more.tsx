import { View } from "react-native";

/** Placeholder: the tab is present in the design but has no content yet. */
export default function MoreScreen() {
  return <View className="flex-1 bg-[#f7f9fb]" />;
}
